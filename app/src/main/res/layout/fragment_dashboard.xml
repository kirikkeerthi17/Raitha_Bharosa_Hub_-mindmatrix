<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Title -->
        <TextView
            android:text="Raitha Bharosa Hub"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp"/>

        <!-- Weather Card -->
        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#4CAF50"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp">

            <TextView
                android:text="Weather"
                android:textColor="#FFFFFF"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/tempText"
                android:text="28°C"
                android:textColor="#FFFFFF"
                android:textSize="20sp"/>

            <TextView
                android:text="Partly Cloudy"
                android:textColor="#FFFFFF"/>
        </LinearLayout>

        <!-- Soil Status Card -->
        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#FFFFFF"
            android:elevation="4dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp">

            <TextView
                android:text="Soil Status"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/moistureText"
                android:text="Moisture: 25%"/>

            <TextView
                android:id="@+id/nitrogenText"
                android:text="Nitrogen: 40"/>
        </LinearLayout>

        <!-- Recommendation Card -->
        <LinearLayout
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#FFF3E0"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="12dp">

            <TextView
                android:text="Recommendation"
                android:textStyle="bold"/>

            <TextView
                android:id="@+id/recommendationText"
                android:text="Waiting for input..."/>
        </LinearLayout>

        <!-- Action Buttons -->
        <Button
            android:id="@+id/goInputBtn"
            android:text="Enter Soil Data"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"/>

    </LinearLayout>
</ScrollView>